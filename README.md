# Auction App

Fullstack project: React + TypeScript + Redux Toolkit + Spring Boot + MySQL.

## Requirements
- Docker + Docker Compose
- Java 17+
- Node.js 18+ (recommended 20)

## Run MySQL (Docker)
```bash
docker compose -f infra/docker-compose.yml up -d
